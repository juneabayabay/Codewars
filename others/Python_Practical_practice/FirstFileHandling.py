with open("data.txt", "r") as file: # data.txt is the name of the file 
  content = file.read() # "r" meaning read that is the mode

print(content) # they create a new variable name which si the content

# the get the file set of line
