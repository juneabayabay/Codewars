try:
  number = int("Hello")

except ValueError as error:
  print ("Error", error
  )

# Handling error the result of this is error through the string data instread of integer
# ValueError as erro will be handle the Result
